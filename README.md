Untuk menjalankan aplikasi ini perlu menginstall :
- install monggo db community server

Environtment
- pip install -r requirements.txt
- pip install easyocr
- pip install flask
- pip install pymongo
- pip install passlib
